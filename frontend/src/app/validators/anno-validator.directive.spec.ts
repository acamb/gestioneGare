import { AnnoValidatorDirective } from './anno-validator.directive';

describe('AnnoValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new AnnoValidatorDirective();
    expect(directive).toBeTruthy();
  });
});

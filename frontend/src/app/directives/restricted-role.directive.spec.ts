import { RestrictedRoleDirective } from './restricted-role.directive';

describe('RestrictedRoleDirective', () => {
  it('should create an instance', () => {
    const directive = new RestrictedRoleDirective();
    expect(directive).toBeTruthy();
  });
});

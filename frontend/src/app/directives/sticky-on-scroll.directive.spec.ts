import { StickyOnScrollDirective } from './sticky-on-scroll.directive';

describe('StickyOnScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new StickyOnScrollDirective();
    expect(directive).toBeTruthy();
  });
});
